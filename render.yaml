services:
  - type: web
    name: file-tracking-system
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate
    startCommand: gunicorn fms.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
